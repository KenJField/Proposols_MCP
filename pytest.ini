[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (requires database connection)
    unit: marks tests as unit tests (no external dependencies)
addopts = 
    -v
    --strict-markers
    --tb=short
